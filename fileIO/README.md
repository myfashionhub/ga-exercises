# File IO

Write a web app. The root directory should show a welcome message. An HTTP GET request to `/links` should read all the links from `links.csv` and display clickable links to all those pages.

Ruby/Python.
